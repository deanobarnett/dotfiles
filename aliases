# Unix
alias ls="exa"
alias l="ls -al"
alias ln="ln -v"
alias mkdir="mkdir -p"
alias v="nvim"
alias c="cd"
alias dev="cd ~/Developer/src/github.com/KanisiTech/rotor-web"

# Docker
alias dc="docker-compose"

# Git
alias gl="git log --decorate --oneline --graph -10"
alias gaa="git add --all"
alias gco="git checkout"
alias gd="git diff"
alias gdc="git diff --cached"
alias gcm="git commit -vS"
alias gp="git push"
alias gpf="git push -f"
alias pull-master="git pull --rebase origin master"
alias gplm="git pull --rebase origin master"

# Ruby
alias b="bundle"
alias spec="rspec"
alias lint-staged="git diff --cached --stat --name-only | xargs rubocop -A"

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'

# Common files
alias todo="nvim ~/TODO.md"

# Include custom aliases
[[ -f ~/.aliases.local ]] && source ~/.aliases.local
